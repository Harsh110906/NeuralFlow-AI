import { test, expect } from '@playwright/test';

test.describe('Agent Basics (PR Suite)', () => {
  test('User can create an agent and edit system prompt', async ({ page }) => {
    expect(true).toBeTruthy();
  });
});
